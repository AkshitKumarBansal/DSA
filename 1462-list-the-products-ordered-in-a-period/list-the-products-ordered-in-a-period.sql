# Write your MySQL query statement below
Select p.product_name, SUM(o.unit) as unit from Products p join Orders o on p.product_id = o.product_id where MONTH(o.order_date) = 2 and YEAR(o.order_date) = 2020 group by p.product_id having SUM(o.unit)>=100;